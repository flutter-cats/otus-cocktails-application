name: cocktail_db_http_using
description: Dart Async lesson
version: 0.0.1

environment:
  sdk: ">=2.14.0 <3.0.0"

dependencies:
  http: ^0.13.3
  built_value: ^8.1.0
  json_serializable: ^4.1.3
  built_collection: ^5.1.0
  dio: ^4.0.0
  retrofit: ^2.0.0
  json_annotation: ^4.0.1
  pretty_dio_logger: ^1.1.0
  # обратите внимание - я могу указать зависимость на репозиторий из git с указанием отдельной ветки
  # и конкретный путь к фолдеру/директории
  cocktail_app_models:
    git:
      url: https://github.com/flutter-cats/otus-cocktails-application.git
      ref: master
      path: lesson_03/lesson/cocktail_app_models/

dev_dependencies:
  retrofit_generator: ^2.0.0
  pedantic: ^1.9.0
  build_runner: ^2.1.0