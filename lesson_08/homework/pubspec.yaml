name: cocktail
description: Dart Async lesson
version: 0.0.1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  json_annotation: ^4.1.0
  flutter_svg: ^1.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  pedantic: ^1.11.1
  http: ^0.13.3
  build_runner: ^2.0.6
  json_serializable: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
