import 'package:flutter/material.dart';

class AppColors {
  // Text colors
  static const primaryTitle = Colors.white;
  static const secondaryTitle = Color.fromRGBO(177, 175, 198, 1);
  static const primaryText = Colors.white;
  static const secondaryText = Color.fromRGBO(234, 234, 234, 1);
  static const hintText = Color.fromRGBO(177, 175, 198, 1);
  static const errorText = Colors.red;
  // App colors
  static const background = Color.fromRGBO(21, 21, 28, 1);
  static const backgroundHint = Color.fromRGBO(26, 25, 39, 1);
  static const primary = Color.fromRGBO(32, 31, 44, 1);
  static const surface = Colors.black;
  static const border = Color.fromRGBO(45, 44, 57, 1);
  static const borderHint = Color.fromRGBO(33, 29, 34, 1);
  static const select = Color.fromRGBO(21, 21, 28, 1);
  static const active = Color.fromRGBO(59, 57, 83, 1);
}
