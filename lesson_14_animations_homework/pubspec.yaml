name: lesson_14_animations_homework
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  json_annotation: ^4.0.0
  http: ^0.13.0
  cupertino_icons: ^1.0.0
  flutter_svg: ^0.22.0

  # обратите внимание - я могу указать зависимость на репозиторий из git с указанием отдельной ветки
  # и конкретный путь к фолдеру/директории
  #  lesson_14_animations_homework:
  #    git:
  #      url: git@github.com:flutter-cats/otus-cocktails-application.git
  #      ref: master
  #      path: lesson_03/lesson/lesson_14_animations_homework/

dev_dependencies:
  flutter_test:
    sdk: flutter
  pedantic: ^1.9.0

  build_runner: ^1.12.2
  json_serializable: ^4.0.3

flutter:
  uses-material-design: true

